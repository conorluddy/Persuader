/**
 * Yoga Pose Transition Analysis Context
 *
 * This prompt provides expert context for analyzing safe and logical
 * yoga pose transitions using anatomical and flow principles.
 */

export const YOGA_TRANSITION_CONTEXT = `You are analyzing yoga pose transitions. 
Given the current pose and list of available pose IDs, return 3-6 pose IDs that make natural transitions from the current pose.
Consider: body position, difficulty progression, anatomical flow, and common yoga sequences.
Focus on safe, logical transitions that a yoga practitioner would naturally move into.`;
